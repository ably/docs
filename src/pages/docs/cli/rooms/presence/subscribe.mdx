---
title: "Rooms presence subscribe"
meta_description: "Subscribe to presence events in an Ably Chat room using the CLI."
meta_keywords: "ably cli, cli, rooms, presence, subscribe, chat, events, ably rooms presence subscribe"
---

Use the `ably rooms presence subscribe` command to subscribe to presence events in a chat room.

## Synopsis

<Code>
```shell
ably rooms presence subscribe <room-name> [flags]
```
</Code>

## Arguments

### `room-name` <a id="room-name"/> <RequiredBadge />

The name of the chat room to subscribe to presence events in.

## Flags

### `--duration | -D` <a id="duration"/>

The duration in seconds to subscribe for before automatically unsubscribing.

### `--client-id` <a id="client-id"/>

A client ID to use for the subscription.

## Global flags

* [`--json`](/docs/cli#json) or [`--pretty-json`](/docs/cli#pretty-json) — JSON output. Mutually exclusive flags.
* [`--verbose`](/docs/cli#verbose) — Enable verbose logging. Can be combined with `--json` or `--pretty-json`.

## Examples

Subscribe to presence events in a room:

<Code>
```shell
ably rooms presence subscribe my-room
```
</Code>

Subscribe for a specific duration:

<Code>
```shell
ably rooms presence subscribe my-room --duration 120
```
</Code>

Subscribe to room presence events in JSON format:

<Code>
```shell
ably rooms presence subscribe my-room --json
```
</Code>

## See also

* [Rooms](/docs/cli/rooms) — Explore all `ably rooms` commands.
* [CLI reference](/docs/cli) — Full list of available commands.
