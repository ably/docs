@Message@ @action@ is a String representing the action type of the message.

```[javascript]
  const MessageActions = [
      'message.create',
      'message.update',
      'message.delete',
      'meta',
      'message.summary'
  ]
```
