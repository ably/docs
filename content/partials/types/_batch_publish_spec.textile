A @BatchPublishSpec@ describes the messages that should be published by a batch publish operation, and the channels to which they should be published.

h4.
  default: Properties

- <span lang="default">channels</span> := The names of the channels to publish the @messages@ to<br>__Type: @String[]@__

- <span lang="default">messages</span> := An array of "@Message@":/docs/api/realtime-sdk/types#message objects<br>__Type: "@Message[]@":/docs/api/realtime-sdk/types#message__
