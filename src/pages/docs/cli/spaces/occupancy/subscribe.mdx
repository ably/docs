---
title: "Spaces occupancy subscribe"
meta_description: "Subscribe to occupancy events on an Ably Space using the CLI."
meta_keywords: "ably cli, cli, spaces, occupancy, subscribe, events, ably spaces occupancy subscribe"
---

Use the `ably spaces occupancy subscribe` command to subscribe to occupancy events on a space.

## Synopsis

<Code>
```shell
ably spaces occupancy subscribe <space-name> [flags]
```
</Code>

## Arguments

### `space-name` <a id="space-name"/> `(Required)`

The name of the space to subscribe to occupancy events on.

## Flags

### `--duration | -D` <a id="duration"/>

The duration in seconds to subscribe for before automatically unsubscribing.

### `--client-id` <a id="client-id"/>

A client ID to use when subscribing.

## Global flags

* [`--json`](/docs/cli#json) or [`--pretty-json`](/docs/cli#pretty-json) — JSON output. Mutually exclusive flags.
* [`--verbose`](/docs/cli#verbose) — Enable verbose logging. Can be combined with `--json` or `--pretty-json`.

## Examples

Subscribe to occupancy events on a space:

<Code>
```shell
ably spaces occupancy subscribe my-space
```
</Code>

Subscribe for a specific duration:

<Code>
```shell
ably spaces occupancy subscribe my-space --duration 60
```
</Code>

Subscribe to space occupancy in JSON format:

<Code>
```shell
ably spaces occupancy subscribe my-space --json
```
</Code>

## See also

* [Spaces](/docs/cli/spaces) — Explore all `ably spaces` commands.
* [CLI reference](/docs/cli) — Full list of available commands.
