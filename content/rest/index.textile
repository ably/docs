---
title: REST Client Library
section: rest
index: 0
---

The Ably REST client library is available in "most popular languages":https://ably.io/download/rest-client-libaries including "Java":https://ably.io/download/rest-client-libraries#java, "Python":https://ably.io/download/rest-client-libraries#python, "Ruby":https://ably.io/download/rest-client-libraries#ruby, ".NET":https://ably.io/download/rest-client-libraries#dotnet, "PHP":https://ably.io/download/rest-client-libraries#php, "Node.js":https://ably.io/download/rest-client-libraries#nodejs, "iOS":https://ably.io/download/rest-client-libraries#iOS and "more...":https://ably.io/download/rest-client-libraries

Whilst you can interact directly with our "REST API":/rest-api/, we advocate the use of "our REST client libraries":https://ably.io/download/rest-client-libaries as they strictly follow our REST API specification, they provide convenience, simplicity, and a number of additional features such as encryption and automatic data center fail-over.  In addition, we have ensured that "our client libraries":https://ably.io/download/client-libaries provide a consistent API across all languages.

h2. Primary interfaces

The client library exposes the Ably service with the following interfaces and methods:

* "Constructor & Usage examples":usage
* "Channels & Messages":channels-messages
* "Presence":presence
* "Authentication":authentication
* "History":history
* "Statistics":statistics
* "Encryption":encryption

h1. REST API Specification

The Ably REST API can also be accessed directly over HTTPS.

p(larger). View the complete "Ably REST API specification":/rest-api

