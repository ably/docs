---
title: "Channels annotations get"
meta_description: "Get annotations for an Ably Pub/Sub channel message using the CLI."
meta_keywords: "ably cli, cli, channels, annotations, get, reactions, ably channels annotations get"
---

Use the `ably channels annotations get` command to get annotations for a channel message.

## Synopsis

<Code>
```shell
ably channels annotations get <channel> <serial> [flags]
```
</Code>

## Arguments

### `channel` <a id="channel"/> `(Required)`

The name of the channel containing the message.

### `serial` <a id="serial"/> `(Required)`

The serial of the message to get annotations for.

## Flags

### `--limit` <a id="limit"/>

The maximum number of annotations to return. Defaults to `100`.

## Global flags

* [`--json`](/docs/cli#json) or [`--pretty-json`](/docs/cli#pretty-json) — mutually exclusive output format flags.
* [`--verbose`](/docs/cli#verbose) — can be combined with either output format flag.

## Examples

Get annotations for a message:

<Code>
```shell
ably channels annotations get my-channel "01234567890:0"
```
</Code>

Get annotations with a limit:

<Code>
```shell
ably channels annotations get my-channel "01234567890:0" --limit 10
```
</Code>

Get annotations and output in JSON format:

<Code>
```shell
ably channels annotations get my-channel "01234567890:0" --json
```
</Code>

Get annotations and output in formatted JSON:

<Code>
```shell
ably channels annotations get my-channel "01234567890:0" --pretty-json
```
</Code>

## See also

* [Channels annotations](/docs/cli/channels/annotations) — Explore all `ably channels annotations` commands.
* [CLI reference](/docs/cli) — Full list of available commands.
