---
title: "Rooms messages update"
meta_description: "Update a message in an Ably Chat room using the CLI."
meta_keywords: "ably cli, cli, rooms, messages, update, chat, ably rooms messages update"
---

Use the `ably rooms messages update` command to update a message in an Ably Chat room.

## Synopsis

<Code>
```shell
ably rooms messages update <room> <serial> <text> [flags]
```
</Code>

## Arguments

### `room` <a id="room"/> <RequiredBadge />

The name of the chat room containing the message.

### `serial` <a id="serial"/> <RequiredBadge />

The serial identifier of the message to update.

### `text` <a id="text"/> <RequiredBadge />

The new text content for the message.

## Flags

### `--description` <a id="description"/>

A description of the update.

### `--headers` <a id="headers"/>

A JSON string of headers to attach to the update.

### `--metadata` <a id="metadata"/>

A JSON string of metadata to attach to the updated message.

### `--client-id` <a id="client-id"/>

A client ID to use when updating the message.

## Global flags

* [`--json`](/docs/cli#json) or [`--pretty-json`](/docs/cli#pretty-json) — JSON output. Mutually exclusive flags.
* [`--verbose`](/docs/cli#verbose) — Enable verbose logging. Can be combined with `--json` or `--pretty-json`.

## Examples

Update a message:

<Code>
```shell
ably rooms messages update my-room "01234567890:0" "Updated message text"
```
</Code>

Update a message with a description:

<Code>
```shell
ably rooms messages update my-room "01234567890:0" "Fixed typo" --description "Corrected spelling"
```
</Code>

Update a message with metadata:

<Code>
```shell
ably rooms messages update my-room "01234567890:0" "Edited message" --metadata '{"edited": true}'
```
</Code>

Update a room message with custom headers:

<Code>
```shell
ably rooms messages update my-room "serial-001" "Updated text" --headers '{"source":"cli"}'
```
</Code>

Update a room message and output the result in JSON format:

<Code>
```shell
ably rooms messages update my-room "serial-001" "Updated text" --json
```
</Code>

## See also

* [Rooms](/docs/cli/rooms) — Explore all `ably rooms` commands.
* [CLI reference](/docs/cli) — Full list of available commands.
