---
title: "Spaces members get"
meta_description: "Get all members in an Ably Space using the CLI."
meta_keywords: "ably cli, cli, spaces, members, get, presence, ably spaces members get"
---

Use the `ably spaces members get` command to get all members in a space.

## Synopsis

<Code>
```shell
ably spaces members get <space-name>
```
</Code>

## Arguments

### `space-name` <a id="space-name"/> `(Required)`

The name of the space to get members for.

## Global flags

* [`--json`](/docs/cli#json) or [`--pretty-json`](/docs/cli#pretty-json) — JSON output. Mutually exclusive flags.
* [`--verbose`](/docs/cli#verbose) — Enable verbose logging. Can be combined with `--json` or `--pretty-json`.

## Examples

Get all members in a space:

<Code>
```shell
ably spaces members get my-space
```
</Code>

Get members in JSON format:

<Code>
```shell
ably spaces members get my-space --json
```
</Code>

Get members in formatted JSON output:

<Code>
```shell
ably spaces members get my-space --pretty-json
```
</Code>

## See also

* [Spaces](/docs/cli/spaces) — Explore all `ably spaces` commands.
* [CLI reference](/docs/cli) — Full list of available commands.
