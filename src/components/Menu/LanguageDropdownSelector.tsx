import React from 'react';

export const LanguageDropdownSelector = ({
  languages,
  showCodeMenu,
  showDefaultLink,
}: {
  languages: string[];
  showCodeMenu: boolean;
  showDefaultLink: boolean;
}) => <></>;
