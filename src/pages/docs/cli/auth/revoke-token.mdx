---
title: "Auth revoke-token"
meta_description: "Revoke an Ably token using the CLI."
meta_keywords: "ably cli, cli, auth, revoke token, token revocation, ably auth revoke-token"
---

Use the `ably auth revoke-token` command to revoke an Ably token.

## Synopsis

<Code>
```shell
ably auth revoke-token <token> [flags]
```
</Code>

## Arguments

### `token` <a id="token"/> `(Required)`

The token to revoke.

## Flags

### `--app` <a id="app"/>

The app ID the token belongs to. Uses the currently selected app if not specified.

### `--client-id | -c` <a id="client-id"/>

The client ID to revoke tokens for.

## Global flags

* [`--json`](/docs/cli#json) or [`--pretty-json`](/docs/cli#pretty-json) — JSON output. Mutually exclusive flags.
* [`--verbose`](/docs/cli#verbose) — Enable verbose logging. Can be combined with `--json` or `--pretty-json`.

## Examples

Revoke a specific token:

<Code>
```shell
ably auth revoke-token "xVLyHw.DRE6hQ..."
```
</Code>

Revoke tokens for a specific client ID:

<Code>
```shell
ably auth revoke-token "xVLyHw.DRE6hQ..." --client-id "user-123"
```
</Code>

Revoke a token and output the result in JSON format:

<Code>
```shell
ably auth revoke-token TOKEN --json
```
</Code>

Revoke a token and output the result in formatted JSON:

<Code>
```shell
ably auth revoke-token TOKEN --pretty-json
```
</Code>

## See also

* [Auth](/docs/cli/auth) — Explore all `ably auth` commands.
* [CLI reference](/docs/cli) — Full list of available commands.
