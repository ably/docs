const { identity } = require("lodash");

const addCompareTable = identity;
const addCompareNames = identity;
const addCompareUrls = identity;
const addCompareId = identity;

module.exports = {
    addCompareTable,
    addCompareNames,
    addCompareUrls,
    addCompareId
}