---
title: "Guide: Building livestream chat at scale with Ably"
meta_description: "Architecting livestream chat with Ably: performance, reliability, and cost at scale. Key decisions, technical depth, and why Ably is the right choice."
meta_keywords: "livestream, chat, scalability, Ably Chat, realtime messaging, dependability, cost optimisation"
---

Ably Chat is purpose-built for livestream chat at scale. If you need to deliver a seamless, high-throughput chat experience to thousands or millions of users—without sacrificing performance, reliability, or cost—Ably is the proven choice. This guide explains the architectural decisions, technical challenges, and unique benefits of building livestream chat with Ably, so you can deliver the best possible user experience, every time.

## Why Ably for livestream chat?

Ably is trusted by organisations delivering chat to millions of users in real time. Our platform is engineered around the four pillars of dependability:

- **Performance:** Ultra-low latency messaging, even at global scale.
- **Integrity:** Guaranteed message ordering and delivery, with no duplicates or data loss.
- **Reliability:** 99.999% uptime SLA, with automatic failover and seamless reconnections.
- **Availability:** Global edge infrastructure ensures users connect to the closest point for optimal experience.
- **Cost at scale:** Server-side batching, efficient connection management, and flexible pricing keep costs predictable—even as your audience grows.

PLACEHOLDER: Diagram showing Ably's four pillars of dependability and global infrastructure

Delivering chat events in real time is key to an engaging experience. Ably's serverless architecture eliminates the need for you to manage websocket servers, automatically scaling to handle millions of concurrent connections without provisioning or maintenance. All of the edge-cases around delivery, failover and scaling - leave that to us and focus on what's important to you and your users.

## What this guide covers

- Key architectural decisions for livestream chat
- How to design for scale and user experience
- Approaches to moderation, rate limiting, and batching
- How Ably's features solve real-world challenges
- Proof points and best practices for production

## Architecting your chat rooms: UX first, scale always

The most important decision you can make is the experience you want users to have. It determines the look and feel of the livestream and ultimately the impression your users leave with. The most important architectural decision therefore is how you structure your chat rooms. With Ably, you are not limited by technology - only by the user experience you want to deliver. With Ably you can deliver experiences that are:

- **Single massive room:** Ably seamlessly supports millions of users in a single room, by automatically distributing websocket connections across multiple servers to ensure endless connection scalability. This is ideal for global events or streams where everyone participates together.
- **Many concurrent rooms:** You can run thousands of rooms in parallel, each with their own audience and message flow. Not only do connections scale horizontally, but Ably uses [consistent hashing](https://ably.com/blog/implementing-efficient-consistent-hashing) to evenly distribute the management of chat rooms across instances, allowing the number of rooms to grow without limit and without causing load spikes when you do.

PLACEHOLDER: Diagram comparing single-room vs. multi-room architectures

In making this decision, there are some **key considerations**:

- Should everyone see the same messages, or do you need to segment by language, region, friend group or topic?
  - If you're running livestreams over multiple languages or geographical regions, you may want to consider separating users similarly so that everybody understands the chat.
  - Alternatively, you may want to make use of translation services, that can translate messages on the fly to the language of the user.
- Do you want an environment where everyone has time to read every message, or the chat to go wild with hype when something exciting happens?
  - At anything more than 5-10 messages per second, the chat becomes unreadable for the audience.
  - **Sharding** is one approach to this problem - separating users into smaller groups so that the chat is more manageable. This can be done randomly, or using some predefined scheme, e.g. language, geographical region.
  - You can also simply _drop_  a proportion of messages on the client to keep it readable. If you're not worried about everyone seeing everything, this is a simple approach.
  - Of course, you can also rate-limit how often users send messages. This controls the rate of messages, but can negatively impact the user experience if they're not able to respond when they want to.
- Do you need to support both public and private chat? If you do, you might want one big room for public chat and a separate room for private messages.

Ably gives you the flexibility to design for your users, not your infrastructure.

## Message throughput and rate management

Livestream chat is defined by high message rates and unpredictable spikes. Ably is engineered to handle these loads without degradation.

- **Proven at scale:** Ably delivers over 500 million messages per day for customers, with individual rooms supporting over 1 million concurrent users.
- **Server-side batching:** Reduce costs and network overhead by grouping messages before delivery. For example, batching messages over a 500ms interval can reduce message costs by up to 8x in high-traffic streams.
- **Rate limiting:** Control the flow of messages per user to maintain readability and prevent spam. Choose between per-user rate limits, global throttling, or adaptive algorithms.

CODE HERE: Example of implementing per-user rate limiting logic

PLACEHOLDER: Visual showing message spikes and how batching smooths delivery/costs

**Decision point:**
- What is the maximum message rate you want users to experience?
- How will you balance real-time delivery with cost efficiency?

## Authentication

Authentication in livestream chat is a design decision. In some cases, you'll want to tie participants to their identity in your application. In others, anyone can join and participate. Ably Chat supports both of these scenarios - you don't need to go through the hassle of setting up users in Ably and trying to keep them in sync with your application. Every client has a clientID. If you want random users, just choose a random string. If you want to tie it to your application, then use the UUID of the user or some other well-known identifier.

How you authenticate is also key. To balance security and experience, you want short-lived tokens that can be easily revoked if a users is misbehaving or needs their permissions changed, but also avoiding the overhead of changing permissions every 2 minutes. In production apps, we strongly recommend you don't use API keys for client-side authentication (server-side is OK) - these are long-lived and much more difficult to revoke in a hurry.

With Ably Chat, authentication is best achieved using JSON Web Tokens (JWTs). These are tied to a particular clientID and come with a set of [capabilities](https://ably.com/docs/chat/setup#authentication) that control what a client can and cannot do. Ably's SDKs handle the timing and process of requesting a new token for your, refreshing it when it expires. All you need to do is provide a server-side endpoint that can generate the JWT for the client. This allows clients to use your existing authentication systems or user sessions to generate their Ably token.


## Moderation: Protecting your community

Effective moderation is essential for livestream chat to maintain a safe and engaging environment, prevent spam and inappropriate content, and ensure compliance with platform guidelines. While human moderators are valuable, they cannot scale to handle the high message volumes of popular livestreams - a single stream can generate thousands of messages per minute. Automated moderation solutions are therefore crucial for real-time content filtering at scale.

Ably supports both inline (before-publish) and after-publish moderation workflows, making it easy to integrate with AI-powered moderation tools.

- **After-publish moderation:** Messages appear instantly, then are removed if flagged. This minimises latency and is suitable for high-velocity chats.
- **Before-publish moderation:** Messages are held until approved. Use this for sensitive streams where content control is paramount.

### Key moderation considerations

When implementing chat moderation, consider these essential factors:

1. **Community standards**
   - What level of moderation is appropriate for your audience?
   - How will you handle different types of content? For example:
     - **Hate speech and harassment:** Detecting discriminatory language, threats, or targeted abuse
     - **Inappropriate content:** Flagging adult content, violence, or graphic material
     - **Toxicity:** Measuring overall message sentiment and hostility
   - AI moderation systems typically classify text according to categories similar to those above, usually with a severity and confidence level. If you have a provider in mind, what thresholds would you set for your chat?
   - Do you need different rules for different user groups?

2. **Technical requirements**
   - How latency-sensitive is your chat?
     - Inline AI moderation can add up to 100ms to message delivery
   - Will you use a third-party moderation service or build your own?

3. **Community-specific approaches**
   - **Gaming streams:** More relaxed rules for casual language and humor
   - **News and current affairs:** Strict moderation of misinformation and hate speech
   - **Educational content:** Emphasis on respectful discussion and spam prevention

### How Ably helps keep your chats safe

Ably's integration with moderation providers allows you to customise the moderation policy on a per-chat basis, allowing you to implement different rules and sensitivity levels for different types of content and communities. You can even configure what happens if the moderation provider can't be reached - for example, publishing the message anyway. Get up and running with **out-of-the box integrations in minutes** and try out many providers for free. Ably takes care of all of the complex bits of invoking the moderation provider, handling the results and failure conditions.

Of course, you may already have built moderation in other parts of your organisation and want to re-use it. Ably's got you covered! "Bring your own" moderation rules allow you to connect your chat rooms to your own infrastructure via popular transports including serverless functions, queues and webhooks. By implementing simple APIs, you can integrate your own moderation solution seamlessly with Ably.

CODE HERE: Example of handling moderation failures in Ably Chat

PLACEHOLDER: Diagram showing moderation flow (user → Ably → moderation → delivery)

## Occupancy: Give volume to your livestream

Ably's occupancy features provide you with realtime statistics of how many users are engaging with your live stream. These can be used to hype up your streams and draw in users to be part of the experience.

- **Within a stream:** Show the number of active users in real time.
- **Across streams:** Aggregate occupancy data for stream lists or dashboards.

CODE HERE: Example of retrieving occupancy metrics

## Reactions, typing indicators, and ephemeral events

Enhance engagement with room reactions, typing indicators, and more. Immortalise the winning goal, the controversial decision and the final whistle by allowing users to express en-masse with a flood of emotions. Ably's Chat SDK provides simple interfaces for adding these features to your livestream.

- **Room reactions:** Broadcast sentiment (e.g., 👍, ❤️) to all users. Not persisted, optimised for scale.
- **Typing indicators:** Show when users are composing messages.

CODE HERE: Example of sending a room reaction

## Message history and context

Some users may join your livestream late, or miss important information. Ably stores chat history for 30 days by default, with options to extend. Retrieve previous messages to provide context for users joining mid-stream.

- **Contextual history:** Show recent messages on join.
- **Deep history:** Allow users to scroll back through older messages in batches.

CODE HERE: Example of fetching message history

## Handling network disruption

Network disruption happens - mobile internet loses signal or someone drives through a tunnel. Ably's SDKs are designed with this in mind.

Every SDK instance keeps track of where it's at in the message stream. If the connection is lost, the library will automatically attempt to reconnect to the servers and in doing so, resume its position in the stream. This allows the chat to continue as if the user never left. After extended periods of disconnection, the client can make use of the chat history API to backfill missing messages.

It's incredibly rare, but sometimes a client might lose connection to a particular data center. Ably operates in multiple data centers around the world with multiple fallback regions available. If a client can't reach the nearest data center, it will try the next one until the connection is re-established, ensuring minimal downtime and that network issues don't disrupt the experience that you are trying to build.

## Priced for scale

Livestreams can be a high-churn affair, with many users coming and going throughout. In Monthly Active Users (MAU) pricing models, this often leads to bills that far outweigh the actual usage. Ably offers a competitive pricing model that is based on consumption, with discounted rates for higher usage. So you won't be billed for users that come and go, only for whilst they're engaging with your experience.

A full breakdown of pricing, as well as a cost estimator for your scenario, can be found on the [pricing page](https://ably.com/pricing?chat-type=sales).

## Cost optimisation at scale

Ably is designed to keep costs predictable, even as your audience grows.

### Server-side message batching

In large-room livestream chats, the majority of the cost comes from delivering messages to all of the participants. It also limits how many messages per second can be processed.

With server-side batching, Ably groups together messages sent in quick succession on the server and fans them out to clients as one unit. This reduces the total amount of messages sent to each client, greatly reducing your bill. All of this is fully configurable - you can control the rollup period to balance responsiveness in your chat versus cost.

Let's calculate the fanout for a chat room with 1000 users, each posting once per minute:

#### Unbatched scenario:

- 1000 users × 1 message/minute = 1000 messages/minute or 16 messages/second
- Total fanout: 1000 × 1000 (assuming the message is echoed back to the publisher)
- Total outbound message rate: **1,000,000 outbound messages/minute** or 16,667 messages/second

#### With 500ms batching:

- Messages are grouped into 500ms batches, so users will see a maximum of 500ms delay from publish to receiving
- In 500ms, we expect: (1000 messages/minute) × (0.5 seconds/60 seconds) ≈ 8.3 messages per batch
- Each batch is delivered once to each user
- Over the space of a minute, there are now only **120,000 outbound messages/minute** or 2000 messages per second

That's an **8x reduction** in the number of messages sent out to clients per minute.

#### Cost grows linearly as you do

The key takeaway is what happens once you increase the number of users to 2000, the inbound message rate goes up to 32/second, which unbatched is 4 million outbound messages/minute. With 500ms batching, the outbound message rate grows to 240,000 (double the amount for 1000 users). Therefore, with Ably's unique server-side batching feature, **your costs grow linearly as you scale**.

### Aggressive connection management

When a client abruptly disconnects from Ably, there is a 2 minute delay before the connection is cleaned up on the server, to allow the client to resume the connection from where it left off. When you're finished with an Ably connection, be sure to call the `close()` method on the client library to gracefully shut down the connection.

All Ably SDKs also perform "heartbeats" with the server to allow detection of dropped or disrupted connections. The default interval for this is 15 seconds. By adjusting the heartbeat interval, you can control how quickly a connection is deemed to have dropped and therefore reduce the amount of time connections remain open for.

- **Server-side batching:** Dramatically reduces message costs in high-throughput scenarios.
- **Aggressive connection management:** Configure heartbeat intervals and close idle connections to minimise resource usage.
- **Flexible pricing:** Choose the package that matches your expected usage. See [Ably pricing](/docs/pricing#packages) for details.

## AI Integrations

AI chatbots are revolutionizing the way we interact with digital platforms. As livestreaming continues to grow, integrating AI capabilities into your chat experience can provide enhanced engagement, automated moderation, and personalized interactions.

Ably's flexible integration capabilities make it easy to connect your chat system with AI services. Through Ably's integration rules, you can automatically route messages to:

- Serverless functions (AWS Lambda, Azure Functions, etc.)
- Webhooks
- Custom endpoints

This enables powerful AI features like:

- Sentiment analysis of chat messages
- AI-powered responses to common questions
- Real-time language translation
- Chat summarization and key point extraction

For example, you could set up an integration rule that sends all messages to an AI service for sentiment analysis, then automatically flags or removes messages that exceed a certain negativity threshold. Or route messages to a language model to generate contextual responses to viewer questions.

The possibilities are endless - Ably's real-time infrastructure ensures your AI integrations can keep up with the pace of livestream chat, while maintaining the reliability and scalability you need.

## Production-ready checklist

Before you go live, review these key points:

- Use token authentication for all client-side communication.
- Apply the principle of least privilege for client permissions.
- Validate your message rate and batching configuration.
- Monitor occupancy and message delivery metrics.
- Confirm you are on the right Ably package for your scale.

## Next steps

- Explore the [Ably Chat product documentation](https://ably.com/docs/chat) for API details and advanced features.
- Review [rooms and messages](/docs/chat/rooms), [moderation](/docs/chat/rooms/moderation), and [server-side batching](/docs/messages/batch#server-side).
- Contact Ably for architectural reviews or to discuss your specific requirements.

PLACEHOLDER: Didn't find what you want? Get help section
