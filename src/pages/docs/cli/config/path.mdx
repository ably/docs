---
title: "Config path"
meta_description: "Print the path to the Ably CLI config file."
meta_keywords: "ably cli, cli, config, path, configuration file, ably config path"
---

Use the `ably config path` command to print the path to the Ably CLI config file.

## Synopsis

<Code>
```shell
ably config path [options]
```
</Code>

## Options

### `--json` <a id="json"/>

Output results as compact JSON. Mutually exclusive with `--pretty-json`.

### `--pretty-json` <a id="pretty-json"/>

Output results in formatted JSON. Mutually exclusive with `--json`.

### `--verbose | -v` <a id="verbose"/>

Enable verbose logging. Can be combined with `--json` or `--pretty-json`.

## Examples

Print the config file path:

<Code>
```shell
ably config path
```
</Code>

Open the config file in VS Code:

<Code>
```shell
code $(ably config path)
```
</Code>

Open the config file in Vim:

<Code>
```shell
vim $(ably config path)
```
</Code>

Print the config file path in JSON format:

<Code>
```shell
ably config path --json
```
</Code>

## See also

* [Config](/docs/cli/config) — Explore all `ably config` commands.
* [CLI reference](/docs/cli) — Full list of available commands.
